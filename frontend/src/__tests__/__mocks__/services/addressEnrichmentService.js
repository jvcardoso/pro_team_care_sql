// Mock para services/addressEnrichmentService.js para testes
const mockAddressEnrichmentService = {
  enrichAddress: jest.fn(),
  validateAddress: jest.fn(),
};

export default mockAddressEnrichmentService;
